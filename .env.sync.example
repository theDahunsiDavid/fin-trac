# CouchDB Sync Configuration Example
# Copy this file to .env.development or .env.production and configure your values

# Enable/disable sync functionality
VITE_SYNC_ENABLED=true

# CouchDB server configuration
VITE_COUCHDB_URL=http://localhost:5984
VITE_COUCHDB_DATABASE=fintrac

# Authentication (optional - only needed if CouchDB requires auth)
# VITE_COUCHDB_USERNAME=admin
# VITE_COUCHDB_PASSWORD=password

# Sync behavior configuration
VITE_SYNC_INTERVAL=30000          # Auto-sync interval in milliseconds (30 seconds)
VITE_SYNC_BATCH_SIZE=50           # Number of documents to sync in one batch
VITE_SYNC_AUTO_START=false        # Whether to start auto-sync automatically on app load

# Development vs Production Examples:

# Development (local CouchDB):
# VITE_SYNC_ENABLED=true
# VITE_COUCHDB_URL=http://localhost:5984
# VITE_COUCHDB_DATABASE=fintrac-dev
# VITE_SYNC_AUTO_START=false

# Production (remote CouchDB with auth):
# VITE_SYNC_ENABLED=true
# VITE_COUCHDB_URL=https://your-couchdb-server.com
# VITE_COUCHDB_DATABASE=fintrac-prod
# VITE_COUCHDB_USERNAME=your-username
# VITE_COUCHDB_PASSWORD=your-password
# VITE_SYNC_INTERVAL=60000
# VITE_SYNC_AUTO_START=true

# To disable sync entirely:
# VITE_SYNC_ENABLED=false
